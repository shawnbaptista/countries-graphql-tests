export interface Continent {
  code: string;
  name: string;
  capital?: string;
}

export interface Country {
  code: string;
  name: string;
  native?: string;
}

export interface Language {
  code: string;
  name: string;
  native?: string;
}